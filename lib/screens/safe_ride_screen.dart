import 'package:flutter/material.dart';

class SafeRide extends StatelessWidget {
  const SafeRide({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text(
          'ENMUmobile',
        ),
      ),
      body: const Center(
        child: Text('SafeRide Screen'),
      ),
    );
  }
}
