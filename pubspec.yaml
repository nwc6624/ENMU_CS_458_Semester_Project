---
name: enmu_mobile
description: CS458 Semester Project. This app is a university resources app for ENMU.
publish_to: none
version: 1.0.0+1
environment:
  sdk: '>=3.1.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  url_launcher: ^6.2.1
  http: ^0.13.3   # Added the http package
  html: ^0.15.0   # Added the html package

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/safe-ride-logo.png
